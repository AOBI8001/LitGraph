// Shared by API requests, on-demand CLI execution and manual MCP tasks.
export const RESEARCH_PRESENTATION = `Presentation contract (identical for API and external Agent):
The answer string is Markdown, not HTML and not a JSON string encoded twice. Start with a direct answer, then short paragraphs. Use short headings, lists, bold and tables only when they improve clarity; put blank lines around lists and tables. Never wrap the answer in a code fence. Escape line breaks correctly in the enclosing JSON. Do not return raw HTML, embedded scripts, local paths, internal tool logs or private reasoning. Use the requested language consistently. Keep source-grounding and inference labels required above. Write and close the answer field first, then supply three follow-up questions as separate plain-text strings. Completing the evidence-grounded answer takes priority over suggestions. The application renders the same sanitized Markdown for every provider.`;

export const SUMMARY_PRESENTATION = `Presentation contract (identical for API and external Agent):
summary is plain text in short paragraphs, not Markdown, HTML, a bullet list or a fenced block. Explain the research question and method, the substantive findings, and important limitations in that order when supported. Separate paragraphs with JSON-escaped newlines. Do not add a title, a source list or redundant bibliographic metadata. Never invent missing details. Mark each inference explicitly. label, keywords, theory labels and relationship rationale are plain text too. Always complete the required JSON schema; a valid summary does not depend on finding relationships.`;
