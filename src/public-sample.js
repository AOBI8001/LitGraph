import sample from './sample-project.json' with { type: 'json' };

// Curated 50-node graph. The optional MD corpus is attached by the build;
// this metadata module never embeds personal paths or PDF files.
export default sample;
